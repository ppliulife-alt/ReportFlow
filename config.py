class Config:
    """统一配置文件。

    当前项目所有配置都集中维护在这个文件里，
    不再依赖 .env。
    """

    # =========================
    # 豆包 / 火山方舟配置
    # =========================
    # 豆包 API Key
    DOUBAO_API_KEY = "YOUR_DOUBAO_API_KEY"

    # 豆包 Responses API 地址
    DOUBAO_API_URL = "https://ark.cn-beijing.volces.com/api/v3/responses"

    # 豆包接入点 ID
    DOUBAO_MODEL = "YOUR_DOUBAO_ENDPOINT_ID"

    # 豆包请求超时时间，单位：秒
    DOUBAO_TIMEOUT_SECONDS = 60

    # 豆包最大输出 token 数
    # 这里已经调大，适合生成 800 字左右报告
    DOUBAO_MAX_OUTPUT_TOKENS = 2000

    # 温度越低，输出越稳定
    DOUBAO_TEMPERATURE = 0.2

    # =========================
    # 企业微信 webhook 配置
    # =========================
    # 当前不是主流程，保留兼容能力
    WECHAT_WEBHOOK_URL = ""

    # =========================
    # 微信公众号配置
    # =========================
    # 微信公众号 AppID
    WX_GZH_APPID = "YOUR_WX_GZH_APPID"

    # 微信公众号 AppSecret
    WX_GZH_APPSECRET = "YOUR_WX_GZH_APPSECRET"

    # openid 单发测试用配置
    WX_GZH_TEST_APPID = "YOUR_WX_GZH_TEST_APPID"
    WX_GZH_TEST_APPSECRET = "YOUR_WX_GZH_TEST_APPSECRET"
    WX_GZH_TEST_OPENID = "YOUR_WX_GZH_TEST_OPENID"

    # 微信公众号接口基础地址
    WX_GZH_API_BASE = "https://api.weixin.qq.com"

    # =========================
    # Prompt 配置
    # =========================
    # 通用问答接口使用的系统提示词
    SYSTEM_PROMPT = """你是一个行业数据分析助手，请输出结构化内容：

1. 今日行情概述
2. 价格变化原因
3. 市场情绪分析
4. 短期趋势预测（3-7天）

要求：
- 简洁
- 客观
- 分点输出
- 不要废话
- 总字数尽量控制在2000字以内"""

    # 第三个正式接口使用的固定问题
    FIXED_GARLIC_REPORT_QUESTION = (
        "帮我收集今天金乡大蒜的行情(报价)信息，同时预测后续行情走势，汇总成一份800字左右的报告"
    )

    # 第三个正式接口使用的公众号纯文本排版 Prompt
    # 注意：这里故意不用 Markdown，
    # 因为当前公众号发送走的是 text 群发，纯文本最稳。
    GZH_REPORT_PROMPT = """你是一个农产品行情分析助手。请围绕“金乡大蒜今日行情与后续走势”输出一篇适合微信公众号文本消息群发的纯文本报告。

要求：
1. 第一行必须输出标题，标题格式固定为：金乡大蒜行情简报（YYYY年M月D日）
2. 全文总字数控制在 750-900 字
3. 全文分为五部分：
   今日报价
   行情变化原因
   后续走势判断
   风险提示
   简要结论
4. 每一部分的小标题必须单独占一行，并严格使用以下格式：
【今日报价】
【行情变化原因】
【后续走势判断】
【风险提示】
【简要结论】
5. 使用纯文本排版，不要使用 Markdown，不要使用 #、##、*、- 等格式符号
6. 标题下面必须空一行；每个小节之间也必须空一行
7. 每个部分都要单独成段，段落不要过长，适合手机阅读
8. “今日报价”部分请把主要报价拆成多行展示，每行一个品类或规格，不要全部挤在一整段里
9. “行情变化原因”“后续走势判断”“风险提示”每部分控制在 2-4 句
10. 语言简洁、专业、客观，不要口语化，不要废话
11. 结尾必须补一段“【简要结论】”，用一句话概括整体判断
"""

    # openid 单发使用的短报 Prompt
    # 客服文本消息长度有限，因此这里单独压缩成更短版本
    GZH_OPENID_REPORT_PROMPT = """你是一个农产品行情分析助手。请围绕“金乡大蒜今日行情与后续走势”输出一篇适合微信公众号客服消息单发的纯文本简报。

要求：
1. 第一行必须输出标题，格式固定为：金乡大蒜行情简报（YYYY年M月D日）
2. 全文总字数控制在 280-420 字
3. 全文分为四部分：
【今日报价】
【行情原因】
【后续走势】
【结论】
4. 使用纯文本，不要使用 Markdown
5. 每一部分单独成段，适合手机快速阅读
6. 报价部分最多保留 4-5 行关键信息
7. 语言简洁、专业、客观
"""
